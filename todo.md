in sutbask tables, show how much each group is worth?
compact debug mode (only failures: first failing testcase per group + slowest testcase). previous implementation was bad and was dropped; redo from scratch.
